{
  "name": "abyss-the-infinite-below",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:deep": "tsx scripts/deep-validate.ts",
    "verify:live": "node scripts/verify-live.mjs",
    "smoke": "tsx scripts/smoke.ts"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/three": "^0.170.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
